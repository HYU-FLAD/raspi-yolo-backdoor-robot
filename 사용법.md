# Window 환경 설정

1. 하스팟 설정 ON
   1. 모바일 하스팟을 킨다
   2. 속성에서 편집을 들어간다
   3. 이름과 암호, 밴드를 다음과 같이 설정한다
      * 이름: SEPHIROTH
      * 암호: fl123456
      * 밴드: 2.4GHz
2. 네트,워크 어댑터 설정
   1. `win+r` 로 실행창 열어서 `ncpa.cpl` 열기
   2. IP 고정: `192.168.24.1`로 네트워크 대역 고정
   3. 공유 탭에서 “홈네트워킹 연결”→ 이더넷 설정 & 제어 ON
3. 라즈베리 파이 ON
   1. A기계 테스트: `ping 192.168.24.50`
   2. B기계 테스트: `ping 192.168.24.60`

# 실행-단독 실행

1. 코드 파일 open
2. cmd 창에서 run 입력후 enter로 실행
